/**
 * Default settings and options to enable/disable features without recompiling
 */
const DefaultSettings = {
    play_hexagram: -1,
    details_hexagrram: -1,
    book_loaded: false,
    preferences: {
        'enable_gongosound': false
    }
}

export default DefaultSettings;